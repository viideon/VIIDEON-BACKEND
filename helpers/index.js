const { sendEmail, sendForGotEmail, sendResetEmail, shareVideoInEmail } = require("./email");
const helpers = {
  sendEmail,
  sendForGotEmail,
  sendResetEmail,
  shareVideoInEmail,
};
module.exports = {
  helpers,
};
